# 系统架构文档

## 📋 目录

- [系统概述](#系统概述)
- [架构设计](#架构设计)
- [核心模块](#核心模块)
- [数据流](#数据流)
- [技术栈](#技术栈)
- [设计决策](#设计决策)

---

## 系统概述

### 项目简介

本系统是一个基于多智能体的财税法务文档审查系统，采用 RAG（检索增强生成）技术，结合知识图谱和记忆系统，提供智能化的文档审查服务。

### 核心特性

- **多智能体协同**: 财务、税务、法务专家协同工作
- **多租户隔离**: 完全的数据隔离和安全保障
- **知识图谱**: 实体关系网络构建和查询
- **记忆系统**: 工作记忆、情景记忆、语义记忆
- **反思机制**: 自我评估和持续改进
- **报告生成**: 多格式专业报告输出

---

## 架构设计

### 1. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Frontend)                      │
│                    Vue.js + Element Plus                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      API 网关层 (API Gateway)                 │
│                    FastAPI + JWT 认证                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      业务逻辑层 (Business Logic)              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 多智能体系统  │  │  知识库管理  │  │  记忆系统    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Agent框架   │  │  文档解析    │  │  搜索服务    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据访问层 (Data Access)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  PostgreSQL  │  │    MinIO     │  │    Neo4j     │      │
│  │  (关系数据)  │  │  (对象存储)  │  │  (知识图谱)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │    Redis     │  │  Qdrant/Milvus│                       │
│  │   (缓存)     │  │  (向量数据库) │                       │
│  └──────────────┘  └──────────────┘                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      外部服务层 (External Services)           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  智谱 AI     │  │  阿里云 OCR  │  │  短信服务    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 2. 多智能体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      协调器 (Coordinator)                     │
│                    任务分解 + 结果合并                        │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                ↓             ↓             ↓
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │  财务专家    │ │  税务专家    │ │  法务专家    │
        │  (Finance)   │ │   (Tax)      │ │  (Legal)     │
        └──────────────┘ └──────────────┘ └──────────────┘
                │             │             │
                └─────────────┼─────────────┘
                              ↓
                    ┌──────────────────┐
                    │  反思专家        │
                    │  (Reflection)    │
                    └──────────────────┘
                              │
                              ↓
                    ┌──────────────────┐
                    │  冲突检测器      │
                    │  (Conflict)      │
                    └──────────────────┘
                              │
                              ↓
                    ┌──────────────────┐
                    │  报告生成器      │
                    │  (Report)        │
                    └──────────────────┘
```

### 3. Agent 框架架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Agent Factory                           │
│                    (创建不同类型的 Agent)                     │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                ↓             ↓             ↓
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │  ReAct Agent │ │  Plan Agent  │ │ Reflect Agent│
        │  (反应式)    │ │  (规划式)    │ │  (反思式)    │
        └──────────────┘ └──────────────┘ └──────────────┘
                │             │             │
                └─────────────┼─────────────┘
                              ↓
                    ┌──────────────────┐
                    │  Base Agent      │
                    │  (基础能力)      │
                    └──────────────────┘
                              │
                ┌─────────────┼─────────────┐
                ↓             ↓             ↓
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │  LLM Adapter │ │ Tool Manager │ │ Memory System│
        └──────────────┘ └──────────────┘ └──────────────┘
```

---

## 核心模块

### 1. 多智能体系统 (`app/multi_agent_system/`)

**职责**: 协调多个专家智能体完成复杂任务

**核心组件**:

- `coordinator.py`: 协调器，负责任务分配和结果整合
- `task_decomposer.py`: 任务分解器
- `result_merger.py`: 结果合并器
- `conflict_detector.py`: 冲突检测器
- `evidence_validator.py`: 证据验证器
- `rework_controller.py`: 重做控制器
- `report_generator.py`: 报告生成器

**专家智能体**:

- `agents/finance_specialist.py`: 财务专家
- `agents/tax_specialist.py`: 税务专家
- `agents/legal_specialist.py`: 法务专家
- `agents/reflection_specialist.py`: 反思专家

### 2. Agent 框架 (`app/agent_framework/`)

**职责**: 提供通用的 Agent 能力

**核心组件**:

- `core/base_agent.py`: Agent 基类
- `core/react_agent.py`: ReAct 模式 Agent
- `core/plan_agent.py`: Plan-Execute 模式 Agent
- `core/reflect_agent.py`: Reflection 模式 Agent
- `core/agent_factory.py`: Agent 工厂

**LLM 适配器**:

- `llm/base_adapter.py`: LLM 适配器基类
- `llm/zhipu_adapter.py`: 智谱 AI 适配器
- `llm/factory.py`: LLM 工厂

**工具管理**:

- `tools/tool_manager.py`: 工具管理器
- `tools/tool_chain.py`: 工具链
- `tools/hybrid_manager.py`: 混合工具管理器

### 3. 知识库系统 (`app/services/`)

**职责**: 管理文档和知识

**核心组件**:

- `file_service.py`: 文件服务
- `chunk_service.py`: 切块服务
- `hybrid_retriever.py`: 混合检索器
- `unified_retriever.py`: 统一检索器
- `smart_router.py`: 智能路由器

### 4. 记忆系统 (`app/memory_system/`)

**职责**: 管理不同类型的记忆

**核心组件**:

- `memory_manager.py`: 记忆管理器
- `working_memory.py`: 工作记忆
- `episodic_memory.py`: 情景记忆
- `semantic_memory.py`: 语义记忆

### 5. 知识图谱 (`app/knowledge_graph/`)

**职责**: 构建和查询知识图谱

**核心组件**:

- `neo4j_manager.py`: Neo4j 管理器
- `entity_extractor.py`: 实体提取器
- `relation_extractor.py`: 关系提取器
- `coreference_resolver.py`: 指代消解器

### 6. 文档解析 (`app/parsers/`)

**职责**: 解析各种格式的文档

**核心组件**:

- `parser_factory.py`: 解析器工厂
- `text_parser.py`: 文本解析器
- `structured_pdf_parser.py`: PDF 解析器
- `structured_word_parser.py`: Word 解析器
- `excel_parser.py`: Excel 解析器

### 7. 切块策略 (`app/chunkers/`)

**职责**: 将文档切分为合适的块

**核心组件**:

- `chunk_factory.py`: 切块工厂
- `plain_text_chunker.py`: 纯文本切块器
- `markdown_chunker.py`: Markdown 切块器
- `structured_document_chunker.py`: 结构化文档切块器

---

## 数据流

### 1. 文档上传流程

```
用户上传文档
    ↓
文件服务接收
    ↓
MinIO 存储
    ↓
解析器解析
    ↓
切块器切分
    ↓
向量化
    ↓
存储到数据库
    ↓
构建知识图谱（可选）
    ↓
完成
```

### 2. 审查流程

```
用户创建审查任务
    ↓
协调器接收任务
    ↓
任务分解
    ↓
分配给专家智能体
    ↓
并行执行审查
    │
    ├─→ 财务专家审查
    ├─→ 税务专家审查
    └─→ 法务专家审查
    ↓
结果收集
    ↓
冲突检测
    ↓
反思评估
    ↓
重做（如需要）
    ↓
结果合并
    ↓
报告生成
    ↓
返回用户
```

### 3. 检索流程

```
用户查询
    ↓
查询优化
    ↓
智能路由
    │
    ├─→ 向量检索
    ├─→ 关键词检索
    └─→ 知识图谱检索
    ↓
结果合并
    ↓
重排序
    ↓
返回结果
```

---

## 技术栈

### 后端

- **框架**: FastAPI 0.104+
- **语言**: Python 3.9+
- **ORM**: SQLAlchemy 2.0+
- **异步**: asyncio, aiohttp

### 数据库

- **关系数据库**: PostgreSQL 14+
- **向量数据库**: Qdrant / Milvus
- **图数据库**: Neo4j 5.x
- **缓存**: Redis 6.x
- **对象存储**: MinIO

### AI/ML

- **LLM**: 智谱 AI (GLM-4)
- **Embedding**: text-embedding-3
- **OCR**: 阿里云 OCR

### 前端

- **框架**: Vue.js 3
- **UI 库**: Element Plus
- **状态管理**: Pinia
- **HTTP 客户端**: Axios

### 部署

- **容器化**: Docker, Docker Compose
- **Web 服务器**: Uvicorn
- **反向代理**: Nginx（可选）

---

## 设计决策

### 1. 为什么选择多智能体架构？

**原因**:
- 复杂任务需要多领域专业知识
- 并行处理提高效率
- 模块化设计易于扩展
- 专家分工提高质量

### 2. 为什么使用 FastAPI？

**原因**:
- 高性能（基于 Starlette 和 Pydantic）
- 自动生成 API 文档
- 类型提示和验证
- 异步支持

### 3. 为什么选择 PostgreSQL？

**原因**:
- 成熟稳定
- 支持 JSON 和数组类型
- 强大的索引能力
- 支持向量扩展（pgvector）

### 4. 为什么使用 Neo4j？

**原因**:
- 原生图数据库
- 高效的图查询
- 可视化支持
- 丰富的图算法

### 5. 为什么实现记忆系统？

**原因**:
- 提供上下文连贯性
- 个性化用户体验
- 知识积累和复用
- 模拟人类记忆机制

### 6. 为什么采用多租户架构？

**原因**:
- 数据隔离和安全
- 资源共享和成本优化
- 易于扩展
- SaaS 模式支持

---

## 扩展性设计

### 1. 水平扩展

- 无状态 API 服务器
- 数据库读写分离
- 缓存层
- 负载均衡

### 2. 垂直扩展

- 模块化设计
- 插件机制
- 配置驱动
- 依赖注入

### 3. 新增专家

```python
# 1. 创建新的专家类
class NewSpecialist(BaseSpecialist):
    def analyze(self, task):
        # 实现分析逻辑
        pass

# 2. 注册到协调器
coordinator.register_specialist("new", NewSpecialist())
```

### 4. 新增工具

```python
# 1. 定义工具
def new_tool(param1, param2):
    """工具描述"""
    # 实现工具逻辑
    return result

# 2. 注册工具
tool_manager.register_tool("new_tool", new_tool)
```

---

## 性能优化

### 1. 数据库优化

- 索引优化
- 查询优化
- 连接池
- 分区表

### 2. 缓存策略

- 热点数据缓存
- 查询结果缓存
- 会话缓存
- CDN 加速

### 3. 异步处理

- 异步 I/O
- 任务队列
- 批量处理
- 并发控制

---

## 安全设计

### 1. 认证授权

- JWT Token
- 角色权限
- API 密钥
- OAuth 2.0（可选）

### 2. 数据安全

- 多租户隔离
- 数据加密
- 敏感信息脱敏
- 审计日志

### 3. 网络安全

- HTTPS
- CORS 配置
- 速率限制
- 防火墙规则

---

**更新时间**: 2024-03-16  
**版本**: 1.0.0
